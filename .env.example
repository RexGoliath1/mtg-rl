# ForgeRL Email Notifications
# Copy to .env and fill in values. NEVER commit .env to git.

# Recipient email address for training reports (REQUIRED)
FORGERL_NOTIFY_EMAIL=

# SMTP server configuration (defaults shown for Gmail)
FORGERL_SMTP_HOST=smtp.gmail.com
FORGERL_SMTP_PORT=587

# SMTP authentication (REQUIRED for sending)
# For Gmail: use your email + app-specific password (not your main password)
# Generate at: https://myaccount.google.com/apppasswords
FORGERL_SMTP_USER=
FORGERL_SMTP_PASS=

# For AWS deployments:
# Store these in SSM Parameter Store or Secrets Manager
# Example SSM paths:
#   /forgerl/notify-email
#   /forgerl/smtp-host
#   /forgerl/smtp-port
#   /forgerl/smtp-user
#   /forgerl/smtp-pass
#
# Load in userdata script:
#   export FORGERL_NOTIFY_EMAIL=$(aws ssm get-parameter --name /forgerl/notify-email --query Parameter.Value --output text)
#   export FORGERL_SMTP_PASS=$(aws ssm get-parameter --name /forgerl/smtp-pass --with-decryption --query Parameter.Value --output text)
